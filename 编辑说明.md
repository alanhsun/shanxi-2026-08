# 行程项目编辑说明

## 权威文件

- `trip.yaml` 保存行程范围、阶段与发布状态。
- `content/` 保存总览与逐日行程。
- `data/` 保存交通、住宿、预约、预算、来源和图片许可。
- `research/social-validation.md` 保存社区验证。
- `decisions.md` 保存已确认的变更。

不要直接编辑 `dist/`；它由构建脚本重新生成。

## 协作流程

1. 从 `main` 创建分支。
2. 修改源文件；增删景点时同步修改交通、预约、预算和来源。
3. 本地安装 `requirements.lock`，运行：

```powershell
python tools/validate_trip.py .
python tools/build_site.py . --output dist
```

4. 打开 `dist/index.html` 检查页面。
5. 提交 PR。校验通过并由维护者合并后，网页自动更新。

## 隐私

这是公开脱敏项目。不要提交姓名、证件号码、手机号、邮箱、订单号、确认码、房号、二维码、访问令牌或付款信息。只使用“维护者”“同行者”等角色。
